- Removed the `IceStorm.Flush.Timeout` property and the `flush-timeout` parameter of the default IceGrid templates
  for IceStorm. This property had no effect: IceStorm no longer batches events on the server side — a subscriber
  registered with a batch oneway or batch datagram proxy receives each event as a regular oneway or datagram
  message.
